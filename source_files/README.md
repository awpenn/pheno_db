This directory will contain files for loading the database. Must set up DOT-GITIGNORE so no source files are pushed to github. Create a `.gitignore` file in this directory and add the following:
```
*
!README.md
```